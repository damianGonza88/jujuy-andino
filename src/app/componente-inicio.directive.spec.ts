import { ComponenteInicioDirective } from './componente-inicio.directive';

describe('ComponenteInicioDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponenteInicioDirective();
    expect(directive).toBeTruthy();
  });
});
